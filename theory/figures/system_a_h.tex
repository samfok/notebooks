\documentclass[convert={density=300,size=500x200,outext=.png}]{standalone}
% \documentclass{standalone}
\usepackage{tikz}
\usetikzlibrary{shapes, arrows}

\begin{document}
\tikzstyle{block} = [draw, rectangle, fill=blue!20,
  minimum height=3em, minimum width=3em]
\tikzstyle{multiply} = [draw, circle, fill=blue!20, minimum size=2em]
\tikzstyle{point} = [coordinate]
\tikzstyle{input} = [coordinate]
\tikzstyle{output} = [coordinate]
\begin{tikzpicture}[auto, node distance=2cm, >=latex']
  \matrix[row sep=2em, column sep=3em] {
    % first row
    \node [input] (input) {}; & 
    \node [multiply] (B) {$B$}; & 
    \node [point] (sum) {} ; & 
    \node [multiply] (A) {$A$}; & 
    \node [point] (output split) {}; \\
    % second row 
    & & & \node [block] (sys) {$h(t)$}; & \\
  };
  \draw [->] (input) -- node {$u(t)$} (B);
  \draw (B) -- (sum);
  \draw [->] (sum) -- node {$e(t)$} (A);
  \draw (A) -- node {$x(t)$} (output split);
  \draw [->] (output split) |- (sys);
  \draw [->] (sys) -| node {$z(t)$} (sum);
\end{tikzpicture} 
\end{document}
